<h1>Edit Movie Screening for <%= @movie.title %></h1>

<%= render 'form' %>

<%= link_to 'Show', movie_movie_screening_path(@movie, @movie_screening), class: "btn btn-default" %>
<%= link_to 'Back', movie_movie_screening_path(@movie), class: "btn btn-default" %>